/*const userModel = require('../models/user.model');

exports.getUserById = (id) => userModel.findById(id);
exports.getUserByEmail = (email) => userModel.findOne({ email });
exports.updateUserById = (id, data) => userModel.findByIdAndUpdate(id, data, { new: true });
exports.deleteUserById = (id) => userModel.findByIdAndDelete(id);*/